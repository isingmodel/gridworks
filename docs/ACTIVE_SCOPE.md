# 현재 작업 범위

## 상태

**활성 구현 scope가 없다.**

current R2 제품 설정 vertical slice는 완료·검증·문서화됐다. 결과와 증거 상한은
[완료 이력](archive/COMPLETED_HISTORY.md)의 `UX-R2.20`이 소유한다.

다음 구현 전에 이 파일에 하나의 결과물, 범위 밖 항목과 결정론적 완료 검사를 먼저 적어 active scope를
명시적으로 열어야 한다. [남은 작업](NEXT_TASKS.md)은 후보와 순서를 설명하지만 그 자체로 구현 권한을
만들지 않는다.

## 닫힌 범위의 경계

- source-tree current R2 settings와 persistence는 구현됐다.
- product audio, packaged settings 재검증, fresh-install candidate, 사람 UX와 출시 승인은 남아 있다.
- push, PR, merge는 수행하지 않았다.
