# Gridworks — Static Balance Lab 후보 명세

> 상태: 첫 플레이 검증판 뒤에 조건부로 개방하는 개발 도구
>
> 현재 Scope 0에서는 구현하지 않는다.

## 1. 왜 뒤에 만드는가

Static Balance Lab은 게임을 대신 플레이하는 AI가 아니다. 살아남은 규칙의 선택공간이 손으로
검산하기 어려워졌을 때, 전체 시간축을 실행하기 전에 명백히 불가능하거나 지배적인 후보를
빠르게 거르는 비권위 도구다.

첫 질문은 수치가 아니라 사람 행동이다.

> 실제 플레이어가 더 싼 공통 경로와 더 비싼 독립 경로 사이에서 고민하는가?

이 질문은 27개 조합, Pareto 전면이나 결정론적 보고서로 답할 수 없다. 따라서 개발 순서는
다음으로 고정한다.

1. 작은 손계산 표로 단위·용량·예산·최단 완공·의무 활성·경고 여유를 검산한다.
2. [첫 플레이 검증판](SCOPE_0_PLAYABLE.md)을 실제 사람에게 플레이하게 한다.
3. 관찰 뒤에도 필요한 규칙만 최소 시간축 회귀검사로 만든다.
4. 유효 전략과 parameter family가 늘어 손검산을 넘을 때만 이 Lab을 연다.
5. Lab의 shortlist는 권위 시간축과 사람 플레이를 모두 통과해야 한다.

## 2. Scope 0 sanity sheet

Scope 0에서 허용되는 정적 도구는 표 또는 짧은 스크립트 하나뿐이다. 다음 질문에 참/거짓과
산식을 출력하면 충분하다.

- 시작예산으로 각 authored 계획을 발주할 수 있는가?
- 발전소에서 수요처까지 닫힌 경로와 충분한 병목용량이 있는가?
- 모든 필수 공사가 공급기한 전에 끝나는가?
- 의무 활성·첫 계량이 물리적 최단 완공보다 이르지 않은가?
- 정확한 사고 경고 뒤 회랑 상태를 검토할 시간이 남는가?
- 공급 MWh, 미공급 MWh, 판매와 보상 단위가 닫히는가?
- 한 authored 계획이 비용·공기·공급 결과에서 다른 계획을 완전히 지배하지 않는가?

후보 자동생성, 최적화, parameter registry, JSON/CSV 보고서, LLM 정책과 full grid sweep을 붙이지
않는다. sanity sheet가 커지기 시작하면 Scope 0의 규칙이 다시 단순화 대상이다.

## 3. Lab 개방 조건

다음을 모두 충족할 때만 적응형 점검에서 Lab을 다음 개발단위로 승인할 수 있다.

1. Scope 0의 사람 통과 기준을 만족한다.
2. 자동으로 비교할 전략 원형이 최소 3개 남아 있다.
3. 시간축 회귀검사로 검증할 물리·경제 규칙이 이미 구현되어 있다.
4. 손검산 때문에 실제 개발 회전이 막힌 증거가 있다.
5. Lab이 답할 구체적 밸런스 가설과 active knob 최대 3개가 정해져 있다.

“나중에 필요할 것 같음”은 개방 근거가 아니다.

## 4. 권위 경계

- 입력 단위, 비용 산식과 안전 기준은 제품의 기계 권위 데이터를 그대로 읽는다.
- Lab은 저장게임, 캠페인 결과, 플레이어 점수 또는 catalog를 수정하지 않는다.
- 시간에 따른 열, 기동, SOC, 공사, 경고와 정산은 권위 시간축 실행이 최종 판정한다.
- Lab이 안전이라고 했지만 권위 실행에서 실패한 `false-safe`는 회귀 결함이다.
- 제품과 Lab이 같은 산식을 복사하지 않는다. 개방된 Core의 순수 계산만 재사용한다.
- 별도 서비스나 LLM orchestration은 만들지 않는다. 필요한 경우 기존 CLI의 `balance` 명령으로
  시작한다.

재미, 설명 이해, 망설임, 실수 회복과 공정성은 Lab이 판정하지 않는다. 통과는 사람
플레이테스트의 입장권일 뿐 출시 승인이 아니다.

## 5. 파라미터 정책

모든 숫자는 다음 네 종류 중 하나다.

| 종류 | 직접 sweep | 뜻 |
|---|---|---|
| `Structural` | 금지 | 틱 순서, P0 자동차단 금지, 원장 계량 경계 |
| `Derived` | 금지 | 길이에서 얻은 표시 철탑 수, MWh 변환, usable 배터리 내부용량 |
| `BalanceKnob` | 허용 | 사람 관찰로 열린 비용·수요·보상 계열 |
| `PresentationOnly` | 금지 | 경보 점멸, 카메라와 UI 애니메이션 |

고정값도 검증 완료를 뜻하지 않는다. 각 숫자는 별도 증거 상태를 가진다.

- `UnverifiedFixture`: 구현·손검산을 위한 시작값
- `ObservedSupport`: 사람 또는 권위 회귀가 가설을 지지함
- `ReviewRequired`: 결과에 민감하거나 모델과 충돌함

한 milestone에서 자동 탐색하는 active knob는 최대 3개다. 모든 후속 milestone의 절대 상한은
6개이며 예외가 없다. 새 knob를 열 때는 왜 기존 값을 닫거나 유지하는지 기록한다. 한 A/B
빌드에서는 한 parameter family만 변경한다.

`독립 경로 추가비`, `폭염 수요배수`, `정전 보상배수`를 미리 예약하지 않는다. Lab을 실제로
열 때 사람 관찰을 근거로 active 값을 선정한다. 안전 하드게이트 때문에
보상배수가 선택을 바꾸지 못한다면 active knob로 쓰지 않는다.

## 6. 입력 bundle

한 실험 bundle은 다음을 고정한다.

- `BalanceVersion`, catalog와 scenario 식별값
- 의무 수요 이력과 P0/P1/P2/P3, 동일 등급 `ShedOrder`
- 시작 현금, 시작 자산, 설비 해금시각과 단일 대기열
- 장 시작·종료, 공급기한, 경고, 최종 착수, 사고와 복구의 권위 캘린더
- 후보 설비의 가격·공기·정격과 authored 회랑
- 정상·폭염·고장의 날씨·수요·가용성
- 단일 판매요금, 발전비, 임대, DR, 수리와 고객등급별 보상
- 플레이어가 실제로 이해할 수 있는 손으로 작성한 전략 원형

계약 수락 여부는 변수가 아니다. 1.0 고객은 장 시작 시 자동 발효되는 같은 의무다. 경로
후보도 모든 polyline을 자동 생성하지 않고, 게임의 Route Planner가 낸 유효 후보 또는 authored
회랑만 쓴다.

Lab 개방 시 고려할 전략 원형의 상한 예시는 다음 다섯 개다.

- `CheapestRadial`: 가장 싼 공통 경로와 최소 현지대비
- `IndependentFeeder`: 병원 독립 피더와 분할모선
- `BypassReserve`: 값싼 평시망과 임시 우회수단
- `DemandFlex`: 고정 공장 DR을 사전무장한 계획
- `PreventiveMaintenance`: 최종 착수 전에 정비를 완료하고, 대상 회선만
  `[start, completion)` 동안 사용불가로 둔 계획정지 중 공급을 검증한 계획

실제 플레이에서 사용되지 않거나 이해되지 않은 원형은 자동 도구에 넣지 않는다.

## 7. 정적 스냅샷

개방된 시스템만 평가한다. 모든 후보를 처음부터 여섯 상태에 강제로 맞추지 않는다. 시간축
물리와 위기 대응이 모두 열린 뒤의 최대 스냅샷은 다음과 같다.

| 경계 | 검사 목적 |
|---|---|
| 정상 피크 | 의무부하, 병목, 판매·발전비와 정상 여유 |
| 폭염 피크 | 자산종류별 기상계수, 수요 증가와 발전 감용 |
| 단일고장 직후 | 살아 있는 경로, Grid Reserve와 병원 P0 연속성 분리 |
| 사고 +15분 | grid-following 배터리, 대기 피커와 사전무장 DR |
| 사고 +2시간 | usable SOC, 발전 여유와 지속 가능한 공급 |
| 복구·결산 | 수리, 보상, 잃은 판매, 기말 현금과 복구시간 |

다음 세 수치를 섞지 않는다.

- `GridReserve`: 온라인 grid-forming 발전원이 있는 섬에서 실제로 낼 수 있는 가스 여유,
  아래 SOC·지속시간 한도를 적용한 grid-following 배터리, 각 지연 자원별 첫 10분 bridge가
  확보된 경우에만 시간 안에 도달하는 grid-following 대기 피커와 사전무장 DR
- `HospitalP0Continuity`: E1/E2 절체, UPS와 고객 비상 디젤
- `BlackoutRecovery`: grid-forming 발전원과 고정 재기동 절차

병원 UPS·디젤은 Grid Reserve나 전력판매로 세지 않는다. 경로 의존성이 정적 스냅샷으로
확정되지 않으면 `NeedsTimelineValidation`으로 표시하며 안전으로 추정하지 않는다.

```text
BatteryEligibleMW(h), h > 0
  = 같은 섬에 온라인 grid-forming 전원이 있으면
      min(MaxDischargeMW, UsableSocMWh / hHours)
    아니면 0
```

15분 스냅샷은 피커나 DR의 10분 도착시각만 보고 통과시키지 않는다. 사고 시작 경계에 요청한
각 지연 자원마다 `0~10분 bridge`와 `10~15분 replacement`가 모두 연속 공급을 만족해야 한다.
늦게 호출했다면 도착시각을 같이 늦추며, 정적으로 증명하지 못하면
`NeedsTimelineValidation`이다.

## 8. 탐색과 공식 목표

탐색은 LLM이 점수가 오를 때까지 한 점씩 바꾸는 과정이 아니다.

1. 단위·산식·일정 가능성을 closed-form으로 검사한다.
2. active knob마다 사전 승인한 하·중·상 값만 둔다. 세 개면 최대 `3³ = 27` 조합이다.
3. 손으로 작성한 전략 원형과 유효 authored 경로만 조합한다.
4. 안전·서비스·지급능력 게이트를 먼저 적용한다.
5. 통과 집합 안에서 기말 현금을 비교하고 ENS·복구·총지출을 진단축으로 표시한다.
6. 경계 후보와 서로 다른 원형을 대표하는 소수만 권위 시간축에 넘긴다.

```text
공식 경제 순위 = 안전 게이트 통과 뒤 기말 현금 내림차순
순캠페인 비용 = 시작 현금 - 기말 현금
```

병원 정전과 돈을 하나의 가중합 점수로 교환하지 않는다. 장기자산 잔존가치를 계산하지 않는
짧은 캠페인에서는 임시수단이 구조적으로 유리할 수 있다. Lab은 이를 `TemporaryMeansDominance`
경고로 표시하고, 금융 파라미터를 추가하는 대신 캠페인 기간·자산 범위·목표를 재검토하게 한다.

## 9. 보고 지표와 자동 경고

각 후보는 다음을 사람이 읽을 수 있는 표로 낸다.

- 안전 통과 여부와 최초 실패 규칙
- P0/P1 미공급 에너지, 전체 ENS와 연속 정전시간
- 정상·사고 병목과 최대 부하율
- Grid Reserve, Hospital P0 Continuity, Blackout Recovery의 별도 상태
- 배터리 최저 usable SOC, 피커 도착시각과 DR 상태
- 시작 현금, 최저 현금, 판매, 총지출, 잃은 판매, 정전 보상과 기말 현금
- 완공일, 공급기한과 위기 전에 실제 사용할 수 없는 공사
- 단일고장·공통원인 사건별 복구시간
- 기준값 대비 민감도와 결과가 뒤집히는 threshold

다음은 자동 경고다.

- 안전한 설계가 0개 또는 1개뿐임
- 한 전략이 모든 스트레스에서 더 싸고 더 안전하며 더 빠름
- 필수 공급기한보다 물리적 최단 완공일이 늦음
- 해금 뒤 위기까지 의미 있는 선택시간이 없음
- 장기 프로젝트가 단일 대기열을 막아 모든 필수 대응이 불가능함
- 위기 전에 완공할 수 없는 자산을 대응안으로 표시함
- 예방정비 경고가 최종 착수시각보다 늦음
- 1% 국소 변화로 결과가 뒤집히지만 UI가 경계를 설명하지 못함
- 정적 안전 후보가 권위 실행에서 실패함
- 단기 임대·임시 설비가 모든 영구 설비를 지배함
- 자동 원형은 성공하지만 사람이 같은 정보를 보고 반복 실패함

## 10. 실행 예산과 결정론

Lab을 열 때 기준 개발 PC와 commit을 보고서에 적고, 실행 예산은 실제 bundle 크기에 맞춰
게이트에서 정한다. “27개 2초”, “12개 캠페인 3분” 같은 수치를 구현 전 제품 약속으로 두지
않는다. 먼저 profile하고 중복 계산과 I/O를 줄이며, 권위 규칙을 바꿔 벤치마크를 맞추지 않는다.

결정론 계약은 다음 수준으로 나눈다.

- 같은 build/runtime/architecture: 같은 입력 보고서는 byte-identical
- 지원 플랫폼 사이: 양자화된 게임 판정·원장·사건 결과가 동일
- raw `double` solver 바이트와 파일 hash의 플랫폼 간 동일성: 기술 spike로 입증하기 전 비약속

ID 정렬은 결정론을 위한 tie-break에만 쓰고 물리 의미를 만들지 않는다. 동일 그래프의
`AssetId`를 일대일로 바꿔도 물리·경제 결과가 같다는 property test를 둔다. solver 비수렴,
NaN이나 예상 밖 수치 오류는 플레이어 정전·보상으로 처리하지 않고 시뮬레이션을 멈춰 진단
fixture를 저장한다.

## 11. 사람 중심 calibration loop

한 튜닝 라운드는 다음 순서를 지킨다.

1. 관찰 근거가 있는 한 문장 가설을 쓴다.
2. 관련 parameter family 하나와 허용 범위를 연다.
3. Lab sweep 한 번으로 불가능·지배 후보를 제거한다.
4. 소수 shortlist를 권위 시간축으로 검증한다.
5. 사람에게 후보 순서를 바꿔 플레이하게 하고 설명·행동·반응시간을 기록한다.
6. 문제와 직접 관련된 국소 sweep을 최대 1~2회만 추가한다.
7. 결정값, 기각 대안, 사람 관찰과 남은 위험을 `BalanceDecision`으로 기록한다.

ghost policy나 플레이어 모사 AI는 만들지 않는다. 동의받은 실제 플레이 입력은 저장·리플레이
게이트가 열린 뒤에만 회귀입력으로 쓸 수 있고, 실패 명령을 자동 교정하지 않는다. LLM은
가설·단위 경계·테스트·요약을 제안할 수 있지만 catalog 쓰기, 무제한 반복, 최종 수치 선택과
출시 승인을 맡지 않는다.

## 12. 원전·데이터센터 확장 경계

원전·데이터센터·냉각수는 1.0 bundle에 넣지 않는다. 해당 milestone을 실제로 열면 별도 물
최적화 도구 없이 다음 파생값만 추가한다.

- 수원지 geometry까지의 직선거리와 수랭 hard cut
- 거리에 선형 비례하는 냉각 펌프 보조전력
- 원전과 수랭 데이터센터의 단일 `CoolingWaterZone` 용량 합계
- 데이터센터 서비스분율·냉각상태별 최종 계통수요와 냉각수
- 폭염·가뭄 때 원전 목표출력, 공랭 전환과 계약 제한 결과

확장값은 지금 모두 `TBD`다. 처음 여는 active knob도 3개 이하이며 1.0 knob와 합쳐 동시에
돌리지 않는다. 정확한 경계는 [1.0 이후 방향](POST_1_0.md)을 따른다.

## 13. 첫 Lab 완료조건

Lab이 별도 게이트로 승인된 뒤의 첫 버전은 다음만 만족하면 된다.

1. 같은 환경에서 같은 bundle은 byte-identical 요약을 만든다.
2. 단위 오류, 범위 밖 knob와 Structural/Derived 직접 sweep을 거부한다.
3. 열린 시스템의 스냅샷과 승인된 전략 원형만 실행한다.
4. 안전 제약을 기말 현금보다 먼저 적용하고 최초 실패 이유를 설명한다.
5. 일정 불가능, 지배전략, 임시수단 지배와 민감한 threshold를 경고한다.
6. 권위 시간축으로 넘길 shortlist manifest를 만들 수 있다.
7. 권위 결과 hash import와 플레이테스트 비교는 해당 시스템이 이미 열렸을 때만 완료조건이다.
8. 도구만으로 재미 또는 출시 통과를 선언할 수 없다.
