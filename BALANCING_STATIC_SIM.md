# Gridworks — Static Balance Lab 명세

## 1. 목적

Static Balance Lab은 게임을 대신 플레이하는 AI가 아니다. 전체 1분 시뮬레이션을 실행하기 전에
수천 개의 수치 조합과 설계 후보를 짧은 시간에 검산해, 명백히 불가능하거나 한 선택만 남는
밸런스를 제거하는 비권위 개발 도구다.

이 도구가 답해야 하는 질문은 세 가지다.

1. 주어진 예산·공기·설비로 정상과 지정 사고의 의무부하를 물리적으로 감당할 수 있는가?
2. 안전한 해법이 하나가 아니라 비용·복구·운영 위험이 다른 두 개 이상 존재하는가?
3. 어느 파라미터가 결과를 크게 바꾸며, 실제 플레이테스트에서 먼저 확인해야 하는가?

재미, 설명 이해, 경보를 읽는 속도, 실수 회복과 감정은 판정하지 않는다. Lab 통과는 사람
플레이테스트의 입장권이지 출시 승인이 아니다.

## 2. 권위 경계

- 입력 catalog, 단위, 비용 산식과 안전 기준은 제품의 기계 권위 데이터와 동일하게 읽는다.
- Lab 결과는 저장게임, 리플레이, 점수 또는 플레이어 화면을 변경하지 않는다.
- 열 관성, 기동, SOC, 공사 진행과 정산의 최종 판정은 권위 1분 Headless 실행이 맡는다.
- Lab이 안전이라고 한 후보가 권위 실행에서 실패하는 `false-safe`는 회귀 결함으로 기록한다.
  반대로 Lab이 보수적으로 탈락시킨 후보는 이유와 오차 경계를 보고서에 남긴다.
- 제품 런타임과 Lab이 같은 산식을 복사해 각각 구현하지 않는다. `Gridworks.Core`의 순수 계산과
  data schema를 공유하고, Lab은 스냅샷 조합과 보고서만 담당한다.

초기 구현은 별도 서비스나 LLM orchestration 없이 `Gridworks.Headless balance` 명령으로 둔다.
성능이나 배포 필요가 생기기 전에는 별도 프로젝트를 추가하지 않는다.

## 3. 파라미터 registry

모든 숫자는 다음 네 종류 중 하나여야 한다.

| 종류 | 직접 sweep | 예 |
|---|---|---|
| `Structural` | 금지 | 1분 권위 틱, 부하 우선순위, 정수 에너지 단위 |
| `Derived` | 금지 | 노선 길이에서 계산한 철탑 수, MWh 환산, CRF |
| `BalanceKnob` | 허용 | CAPEX, 수요배수, 수리시간, 보상 단가 |
| `PresentationOnly` | 금지 | 경보 점멸, 카메라 이동, UI 애니메이션 |

각 `BalanceKnob`는 ID, 단위, 기본값, 최소·최대, sampling scale, milestone, 변경 이유와 관련
안전 기준을 가진다. 범위 밖 값이나 두 단위의 암묵 변환은 schema 단계에서 거부한다.

```json
{
  "id": "assets.temporary_bypass.capex_m",
  "kind": "BalanceKnob",
  "unit": "M",
  "default": 18.0,
  "min": 14.0,
  "max": 24.0,
  "sampling": "linear",
  "milestone": "vertical-slice",
  "reason": "안전 우회선과 보상 노출의 비용 간격 조정"
}
```

한 milestone의 active knob는 8~15개만 허용한다. 나머지는 기준 version에 고정한다. 한 실험이
여러 parameter family를 동시에 바꿔 원인을 알 수 없게 하지 않는다.

## 4. 입력

한 실험 bundle은 다음을 고정한다.

- `BalanceVersion`, catalog와 scenario hash
- 시작 현금, 신용, 이미 존재하는 설비와 공사 가능기간
- 의무 수요와 P0/P1/P2/P3 우선순위
- 후보 발전·저장·송전·변전 설비와 가격·공기·정격
- 정상, 폭염과 고장 시의 기상·수요·가용성
- 계약 단가, 보상, O&M, 연료·탄소와 지정 스트레스 비용 규칙
- 플레이어가 실제로 선택할 법한 손으로 작성한 전략 원형

Lab이 모든 가능한 지도 배치를 생성하려 하지 않는다. 경로 후보는 Route Planner가 만든 유효
후보 또는 디자이너가 의도한 회랑으로 제한하고, 전략 원형은 사람이 이해할 수 있는 이름을
가진다.

초기 전략 원형은 다음 정도면 충분하다.

- `CheapestRadial`: 가장 싼 단일 경로, 최소 현지예비
- `IndependentFeeder`: 병원 독립 피더와 분할모선
- `TemporaryBypass`: 값싼 평시망 + 사전 준비한 임시 우회
- `LocalResilience`: 수요지 배터리·소형 가스 + 제한적 송전 보강
- `DemandFlex`: 공장 DR 계약 + 중간 규모 우회
- `Overbuild`: 비교 기준용 넉넉한 회선과 저장

이들은 최적 정책이 아니라 검산할 설계 언어다. 자동 생성 후보도 최종 보고서에서는 어느
원형에서 무엇이 달라졌는지 설명할 수 있어야 한다.

## 5. 정적 스냅샷

각 설계는 최소 다음 여섯 경계를 통과한다.

| 경계 | 검사 목적 |
|---|---|
| 정상 피크 | 평상시 의무부하, 손실, 수익과 90% 부하율 |
| 폭염 피크 | 수요 증가, 선로 열모델과 변압기 감용, 발전 출력저하 |
| 단일고장 직후 | 살아 있는 경로, UPS·즉시 예비력, P0 연속성 |
| 사고 +15분 | 배터리·온라인 재급전·사전무장 DR과 비상정격 |
| 사고 +2시간 | SOC·연료·기동완료, P1 회복과 지속 가능성 |
| 복구·결산 | 수리비, 보상, 잃은 매출, 지급능력과 평가 LCC |

정적 계산은 각 경계에서 topology connectivity, 손실 보정 DC flow, 정격·열 여유, 공급순서,
저장 에너지 상한과 원장을 평가한다. +15분과 +2시간 사이의 열·SOC 경로 의존성이 모호하면
`NeedsTimelineValidation`으로 표시하고 안전 통과로 추정하지 않는다.

## 6. 탐색 방법

탐색은 값이 좋아질 때까지 LLM이 한 점씩 바꾸는 대화형 시행착오가 아니다.

1. 산식과 단위의 closed-form 검사를 먼저 수행한다.
2. active knob가 적으면 작은 full grid, 많으면 고정 seed의 Latin hypercube 또는 Sobol 표본을
   사용한다.
3. 각 knob를 기준값 주변에서 한 번씩 움직이는 local sensitivity를 계산한다.
4. 안전 게이트를 먼저 적용하고, 통과 집합 안에서 비용·ENS·복구·현금의 Pareto 전면을 만든다.
5. 경계에 가깝거나 서로 다른 전략 원형을 대표하는 후보만 권위 Headless에 넘긴다.

목적함수를 하나의 가중합 점수로 만들지 않는다. 특히 병원 정전과 비용을 환율처럼 맞바꾸지
않는다. P0/P1/N-1/지급능력은 제약이고, 그 안에서 비용과 운영 부담을 비교한다.

## 7. 보고 지표와 경고

각 설계·스냅샷은 다음을 표로 낸다.

- 안전 통과 여부와 최초 실패 규칙
- P0/P1 미공급 에너지, 전체 ENS와 연속 정전시간
- 정상·사고 시 최대 선로/변압기 부하율과 제약 설비
- 즉시·15분·2시간 예비 MW, 배터리 최저 SOC와 연료 지속시간
- 초기 현금, 최저 현금, 평시 순영업 현금, 지정 스트레스 비용과 평가 LCC
- 완공일과 위기 전에 사용할 수 없는 공사
- 단일고장, 공통원인 사건별 복구시간
- 기준값 대비 민감도와 결과를 뒤집는 parameter threshold

다음 상태는 자동 경고다.

- 안전한 설계가 0개 또는 1개뿐임
- 한 전략이 모든 스트레스 케이스에서 더 싸고 더 안전하며 더 빠름
- 가장 싼 안전설계가 가장 싼 위험설계보다 사실상 같은 비용이거나 지나치게 비쌈
- parameter를 1% 바꿨는데 결과가 불연속적으로 뒤집히며 UI가 그 경계를 설명하지 못함
- 정적 안전 후보가 권위 실행에서 반복해 실패함
- synthetic policy만 성공하고 human ghost trace는 같은 정보로 반복 실패함

비용 간격의 “적정” 숫자는 도구가 임의로 정하지 않는다. 초기 목표 범위는 디자이너가
milestone hypothesis로 적고 실제 플레이테스트 뒤 갱신한다.

## 8. 실행 예산

기준 개발 PC와 commit을 보고서에 기록하고 다음을 목표로 한다.

- schema·단위 smoke와 대표 100 variants: 5초 이하
- 10,000 portfolio × 정적 여섯 스냅샷: 2분 이하
- shortlist 50개 이하의 전체 1분 Headless 캠페인: 10분 이하

이는 정확도를 희생해 무조건 맞추는 출시 사양이 아니라 개발 회전 목표다. 초과하면 먼저
중복 계산, topology cache와 보고서 I/O를 profile한다. 권위 시간 모델을 이벤트 도약 방식으로
바꿔 숫자를 맞추지 않는다.

## 9. 사람 중심 calibration loop

한 튜닝 라운드는 다음 순서를 지킨다.

1. 한 문장 가설을 쓴다. 예: “우회선 보험료가 너무 싸서 모든 초보가 고민 없이 산다.”
2. 관련 parameter family 하나와 허용 범위를 연다.
3. Lab sweep 1회로 불가능·지배 후보를 제거한다.
4. 대표 후보 최대 50개를 권위 Headless로 검증한다.
5. 사람에게 후보 A/B를 무작위 순서로 플레이하게 하고 설명·행동·반응시간을 기록한다.
6. 실제 문제와 직접 관련된 local sweep을 최대 1~2회 수행한다.
7. 결정한 값, 기각한 대안, 사람 관찰과 남은 위험을 `BalanceDecision`으로 commit한다.

동의를 받은 플레이 리플레이는 `ObservationId, Command, GameMinute, RealReactionMs`로 익명화해
deterministic ghost policy로 재생한다. ghost는 그 플레이어가 당시에 보지 못한 상태나 미래
사건을 읽지 않고, 실패한 명령도 그대로 보존한다. 사람이 만든 망설임과 실수를 “고쳐서”
최적 정책으로 만들지 않는다.

LLM은 테스트 후보, 단위 경계, 해석 가능한 가설과 보고서 요약을 제안할 수 있다. LLM의
자율 반복 실행, 검증 없이 catalog 값 쓰기, 단일 보상함수 최적화와 “사람도 좋아할 것”이라는
대리 판정은 금지한다. 최종 수치와 출시는 사람이 근거를 읽고 승인한다.

## 10. 완료 기준

Static Balance Lab의 첫 버전은 다음을 모두 만족하면 충분하다.

1. 같은 bundle은 실행 순서와 병렬성에 무관하게 byte-identical JSON/CSV 요약을 만든다.
2. 단위가 다른 값, 범위 밖 knob, 파생값 sweep과 권위 구조값 변경을 거부한다.
3. 여섯 스냅샷과 여섯 전략 원형을 실행하고 최초 실패 원인을 설명한다.
4. 안전 제약을 비용 점수보다 먼저 적용하며 Pareto·민감도·지배전략을 보고한다.
5. shortlist를 권위 Headless 명령으로 넘길 수 있고 결과 hash를 되가져온다.
6. 사람 ghost trace를 미래정보 없이 재생하고 synthetic 결과와 별도 열로 비교한다.
7. 도구 보고서만으로는 재미 또는 출시 통과를 선언할 수 없다.
