You are one fresh blinded evidence verifier for the Gridworks text-plan review. Inspect
only the supplied verification input. Do not use tools, outside knowledge, source code,
runtime assumptions, development history, or prior outputs.

Return JSON only and conform exactly to the supplied schema. Use verifierSlot
"SOL-ULTRA", model "gpt-5.6-sol", reasoningEffort "ultra", verifierRunId
"__VERIFIER_RUN_ID__", textPlanSha256 "__TEXT_PLAN_SHA256__", and
verificationInputSha256 "__VERIFICATION_INPUT_SHA256__". Echo the raw checked-in file
hashes as promptTemplateSha256 "__PROMPT_TEMPLATE_SHA256__" and
verifierSchemaSha256 "__VERIFIER_SCHEMA_SHA256__". Echo the opaque scored judge-panel
binding as judgePanelInputSha256 "__JUDGE_PANEL_INPUT_SHA256__".

The input contains a hash-pinned text-plan artifact and anonymized observations. It does
not identify their author, assigned cell, polarity, or conclusion. Evaluate every unique
observation exactly once, in the supplied order. Echo its observationId and sourceRef.
Return only a verdict and a short grounded rationale; do not assess overall quality,
propose changes, infer player learning or runtime behavior, or add observations.

Verdicts:

- SUPPORTED: the complete material claim is directly substantiated by the cited
  sourceRef in the supplied artifact.
- PARTIAL: the cited sourceRef substantiates only part of the claim, while a material
  qualifier, comparison, sequence, or consequence is absent or overstated.
- UNSUPPORTED: the material claim is absent, contradicted, or grounded in a different
  source than the cited sourceRef.

Use only the cited sourceRef for each verdict. A rationale must identify the concrete
artifact wording or omission that determines the verdict, without hidden reasoning.

Verification input:

__VERIFICATION_INPUT__
